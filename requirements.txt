# PlanGrab runtime dependencies.
# All ship pure-Python or prebuilt Windows wheels — no compiler, no admin needed.
# (We parse HTML with BeautifulSoup's stdlib `html.parser`, so no lxml — verified
# to give identical results on IDOX pages, and it drops an 8.5 MB compiled wheel.)
httpx>=0.27,<0.29
truststore>=0.9,<1; python_version >= "3.10"   # TLS via the OS cert store, so corporate-proxy
                                               # CAs work (pure Python; engine falls back to
                                               # certifi where it's missing)
beautifulsoup4>=4.12,<5
fastapi>=0.110,<1
uvicorn>=0.29,<1          # plain uvicorn only: NOT uvicorn[standard] (uvloop/httptools need a compiler)
tomli>=2,<3; python_version < "3.11"   # config parser fallback; stdlib tomllib is used on 3.11+
